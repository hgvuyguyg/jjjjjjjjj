-- Doors 透视ESP - 增加拉杆透视

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")

local espEnabled = false
local highlights = {}
local showDoors = true
local showKeys = true
local showItems = true
local showMonsters = true
local showLevers = true
local nightVision = true
local rayEnabled = false
local beamData = {}

-- GUI
local gui = Instance.new("ScreenGui")
gui.Name = "ESP"
gui.Parent = game:GetService("CoreGui")

local f = Instance.new("Frame")
f.Size = UDim2.new(0, 250, 0, 340)
f.Position = UDim2.new(0, 50, 0, 100)
f.BackgroundColor3 = Color3.fromRGB(35,35,35)
f.BorderSizePixel = 0
f.Active = true
f.Draggable = true
f.ClipsDescendants = true
f.Parent = gui

local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 35)
titleBar.BackgroundColor3 = Color3.fromRGB(25,25,25)
titleBar.Parent = f

local titleText = Instance.new("TextLabel")
titleText.Size = UDim2.new(0.75, 0, 1, 0)
titleText.BackgroundTransparency = 1
titleText.TextColor3 = Color3.fromRGB(255,255,255)
titleText.Text = "Doors ESP"
titleText.Font = Enum.Font.SourceSansBold
titleText.TextSize = 16
titleText.TextXAlignment = Enum.TextXAlignment.Left
titleText.Parent = titleBar

local isCollapsed = false
local collapseBtn = Instance.new("TextButton")
collapseBtn.Size = UDim2.new(0, 30, 0, 30)
collapseBtn.Position = UDim2.new(1, -32, 0, 3)
collapseBtn.BackgroundColor3 = Color3.fromRGB(60,60,60)
collapseBtn.TextColor3 = Color3.fromRGB(255,255,255)
collapseBtn.Text = "-"
collapseBtn.Font = Enum.Font.SourceSansBold
collapseBtn.TextSize = 20
collapseBtn.Parent = titleBar

local content = Instance.new("Frame")
content.Size = UDim2.new(1, 0, 0, 305)
content.Position = UDim2.new(0, 0, 0, 35)
content.BackgroundTransparency = 1
content.Parent = f

local mainBtn = Instance.new("TextButton")
mainBtn.Size = UDim2.new(0.85,0,0,40)
mainBtn.Position = UDim2.new(0.075,0,0,5)
mainBtn.BackgroundColor3 = Color3.fromRGB(180,40,40)
mainBtn.TextColor3 = Color3.fromRGB(255,255,255)
mainBtn.Text = "ESP: OFF"
mainBtn.Font = Enum.Font.SourceSansBold
mainBtn.TextSize = 16
mainBtn.Parent = content

local function makeBtn(name, y)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.85,0,0,28)
    btn.Position = UDim2.new(0.075,0,0,y)
    btn.BackgroundColor3 = Color3.fromRGB(50,180,50)
    btn.TextColor3 = Color3.fromRGB(255,255,255)
    btn.Text = name..": ON"
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 13
    btn.Parent = content
    return btn
end

local doorBtn = makeBtn("门", 55)
local keyBtn = makeBtn("钥匙", 87)
local itemBtn = makeBtn("道具", 119)
local leverBtn = makeBtn("拉杆", 151)
local monsterBtn = makeBtn("怪物", 183)
local nightBtn = makeBtn("夜视", 215)
local rayBtn = makeBtn("射线", 247)

local warn = Instance.new("Frame")
warn.Size = UDim2.new(0,400,0,80)
warn.Position = UDim2.new(0.5,-200,0.5,-40)
warn.BackgroundColor3 = Color3.fromRGB(255,0,0)
warn.BackgroundTransparency = 0.2
warn.BorderSizePixel = 0
warn.Visible = false
warn.Parent = gui

local warnText = Instance.new("TextLabel")
warnText.Size = UDim2.new(1,0,1,0)
warnText.BackgroundTransparency = 1
warnText.TextColor3 = Color3.fromRGB(255,255,255)
warnText.TextStrokeTransparency = 0
warnText.TextStrokeColor3 = Color3.fromRGB(0,0,0)
warnText.Font = Enum.Font.SourceSansBold
warnText.TextSize = 30
warnText.Parent = warn

collapseBtn.MouseButton1Click:Connect(function()
    isCollapsed = not isCollapsed
    if isCollapsed then
        f:TweenSize(UDim2.new(0, 250, 0, 35), "Out", "Quad", 0.3)
        collapseBtn.Text = "+"
    else
        f:TweenSize(UDim2.new(0, 250, 0, 340), "Out", "Quad", 0.3)
        collapseBtn.Text = "-"
    end
end)

function clearAll()
    for _, d in pairs(highlights) do
        pcall(function()
            if d.hl then d.hl:Destroy() end
            if d.bb then d.bb:Destroy() end
            if d.conn then d.conn:Disconnect() end
        end)
    end
    highlights = {}
    clearBeams()
end

function clearBeams()
    for _, b in pairs(beamData) do
        pcall(function()
            if b.beam then b.beam:Destroy() end
            if b.a0 then b.a0:Destroy() end
            if b.a1 then b.a1:Destroy() end
        end)
    end
    beamData = {}
end

function getPos(obj)
    if obj:IsA("BasePart") then return obj.Position end
    if obj:IsA("Model") then
        local p = obj:FindFirstChildWhichIsA("BasePart")
        if p then return p.Position end
    end
    return nil
end

function addESP(obj, color, name)
    pcall(function()
        local hl = Instance.new("Highlight")
        hl.FillColor = color
        hl.FillTransparency = 0.3
        hl.OutlineColor = Color3.fromRGB(255,255,255)
        hl.OutlineTransparency = 0
        hl.Adornee = obj
        hl.Parent = obj
        hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        
        local bb = Instance.new("BillboardGui")
        bb.Size = UDim2.new(0,200,0,30)
        bb.AlwaysOnTop = true
        bb.Parent = obj
        
        local lb = Instance.new("TextLabel")
        lb.Size = UDim2.new(1,0,1,0)
        lb.BackgroundTransparency = 1
        lb.TextColor3 = Color3.fromRGB(255,255,255)
        lb.TextStrokeTransparency = 0
        lb.TextStrokeColor3 = Color3.fromRGB(0,0,0)
        lb.Font = Enum.Font.SourceSansBold
        lb.TextSize = 16
        lb.Parent = bb
        
        local conn = RunService.Heartbeat:Connect(function()
            if not obj or not obj.Parent then
                conn:Disconnect(); hl:Destroy(); bb:Destroy(); return
            end
            local char = LocalPlayer.Character
            if char and char:FindFirstChild("HumanoidRootPart") then
                local pos = getPos(obj)
                if pos then
                    lb.Text = string.format("%s %.1fm", name, (char.HumanoidRootPart.Position - pos).Magnitude)
                end
            end
        end)
        
        table.insert(highlights, {hl=hl, bb=bb, conn=conn})
    end)
end

function addBeam(targetObj, color)
    pcall(function()
        local char = LocalPlayer.Character
        if not char or not char:FindFirstChild("Head") then return end
        local targetPos = getPos(targetObj)
        if not targetPos then return end
        
        local a0 = Instance.new("Attachment")
        a0.Parent = char.Head
        local a1 = Instance.new("Attachment")
        a1.Parent = targetObj
        a1.WorldPosition = targetPos
        
        local beam = Instance.new("Beam")
        beam.Attachment0 = a0
        beam.Attachment1 = a1
        beam.Color = ColorSequence.new(color)
        beam.Width0 = 0.04
        beam.Width1 = 0.04
        beam.Transparency = NumberSequence.new(0.4)
        beam.FaceCamera = true
        beam.Parent = a0
        
        table.insert(beamData, {beam=beam, a0=a0, a1=a1})
    end)
end

function updateBeams()
    clearBeams()
    if not rayEnabled then return end
    
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("Head") then return end
    local hp = char.Head.Position
    
    for _, obj in ipairs(workspace:GetDescendants()) do
        local pos = getPos(obj)
        if not pos or (hp - pos).Magnitude > 150 then continue end
        
        local color = nil
        if showDoors and obj:IsA("Model") and obj.Name == "Door" then
            color = Color3.fromRGB(50,150,255)
        elseif showKeys and obj.Name == "KeyObtain" then
            color = Color3.fromRGB(255,200,0)
        elseif showLevers and obj.Name:lower():find("lever") then
            color = Color3.fromRGB(255,100,255)
        elseif showItems then
            if obj:IsA("Tool") then color = Color3.fromRGB(50,255,50)
            elseif obj:IsA("Model") then
                local n = obj.Name:lower()
                if n:find("gold") or n:find("coin") or n:find("flashlight") or n:find("lighter") or n:find("vitamin") or n:find("lockpick") or n:find("bandage") or n:find("crucifix") or n:find("revolver") or n:find("shotgun") or n:find("rifle") or n:find("skeletonkey") or n:find("pills") then
                    color = Color3.fromRGB(50,255,50)
                end
            end
        elseif showMonsters and obj:IsA("Model") and not Players:GetPlayerFromCharacter(obj) then
            local n = obj.Name:lower()
            if n:find("rush") or n:find("ambush") or n:find("screech") or n:find("figure") or n:find("seek") or n:find("halt") or n:find("eyes") or n:find("hide") or n:find("dupe") or n:find("shadow") or n:find("granny") or obj:FindFirstChild("Humanoid") then
                color = Color3.fromRGB(255,50,50)
            end
        end
        if color then addBeam(obj, color) end
    end
end

function getMonsterName(obj)
    local n = obj.Name:lower()
    if n:find("ambush") then return "Ambush" end
    if n:find("rush") then return "Rush" end
    if n:find("screech") then return "Screech" end
    if n:find("figure") then return "Figure" end
    if n:find("seek") then return "Seek" end
    if n:find("halt") then return "Halt" end
    if n:find("eyes") then return "Eyes" end
    if n:find("hide") then return "Hide" end
    if n:find("dupe") then return "Dupe" end
    if n:find("shadow") then return "Shadow" end
    if n:find("granny") or n:find("blind") or n:find("library") then return "Figure" end
    if obj:FindFirstChild("Humanoid") then return "怪物" end
    return nil
end

function updateESP()
    clearAll()
    if not espEnabled then warn.Visible = false; return end
    
    local nm = nil
    local nd = 999
    
    for _, obj in ipairs(workspace:GetDescendants()) do
        -- 门
        if showDoors and obj:IsA("Model") and obj.Name == "Door" then
            addESP(obj, Color3.fromRGB(50,150,255), "门")
        end
        -- 钥匙
        if showKeys and obj.Name == "KeyObtain" then
            addESP(obj, Color3.fromRGB(255,200,0), "钥匙")
        end
        -- 拉杆
        if showLevers and obj.Name:lower():find("lever") then
            addESP(obj, Color3.fromRGB(255,100,255), "拉杆")
        end
        -- 道具
        if showItems then
            if obj:IsA("Tool") then
                addESP(obj, Color3.fromRGB(50,255,50), "道具")
            elseif obj:IsA("Model") then
                local n = obj.Name:lower()
                if not n:find("book") and (n:find("gold") or n:find("coin") or n:find("flashlight") or n:find("lighter") or n:find("vitamin") or n:find("lockpick") or n:find("bandage") or n:find("crucifix") or n:find("revolver") or n:find("shotgun") or n:find("rifle") or n:find("skeletonkey") or n:find("pills")) then
                    addESP(obj, Color3.fromRGB(50,255,50), "道具")
                end
            end
        end
        -- 怪物
        if showMonsters and obj:IsA("Model") and not Players:GetPlayerFromCharacter(obj) then
            local mn = getMonsterName(obj)
            if mn then
                local pos = obj:FindFirstChildWhichIsA("BasePart")
                if pos then
                    local char = LocalPlayer.Character
                    if char and char:FindFirstChild("HumanoidRootPart") then
                        local dist = (char.HumanoidRootPart.Position - pos.Position).Magnitude
                        local maxD = (mn=="Rush" or mn=="Ambush") and 300 or 50
                        if dist < maxD then
                            addESP(obj, Color3.fromRGB(255,50,50), mn)
                            if dist < nd then nd = dist; nm = mn end
                        end
                    end
                end
            end
        end
    end
    
    updateBeams()
    
    if nm then
        warn.Visible = true
        if nd < 8 then
            warnText.Text = "⚠ "..nm.." 来了! "..string.format("%.1fm",nd).." ⚠"
            warn.BackgroundColor3 = Color3.fromRGB(255,0,0)
        elseif nd < 25 then
            warnText.Text = "⚠ "..nm.." "..string.format("%.1fm",nd).." ⚠"
            warn.BackgroundColor3 = Color3.fromRGB(255,80,0)
        else
            warnText.Text = nm.." "..string.format("%.1fm",nd)
            warn.BackgroundColor3 = Color3.fromRGB(200,120,0)
        end
    else
        warn.Visible = false
    end
end

spawn(function()
    while true do
        if nightVision then
            pcall(function()
                local a = (Lighting.Ambient.R + Lighting.Ambient.G + Lighting.Ambient.B) / 3
                local o = (Lighting.OutdoorAmbient.R + Lighting.OutdoorAmbient.G + Lighting.OutdoorAmbient.B) / 3
                if a < 0.3 and o < 0.3 then
                    Lighting.Brightness = 5; Lighting.FogEnd = 999999; Lighting.ExposureCompensation = 1.5
                else
                    Lighting.Brightness = 1; Lighting.FogEnd = 500; Lighting.ExposureCompensation = 0
                end
            end)
        end
        task.wait(0.5)
    end
end)

spawn(function()
    while true do
        if espEnabled then updateESP() end
        task.wait(0.5)
    end
end)

mainBtn.MouseButton1Click:Connect(function()
    espEnabled = not espEnabled
    mainBtn.Text = espEnabled and "ESP: ON" or "ESP: OFF"
    mainBtn.BackgroundColor3 = espEnabled and Color3.fromRGB(50,180,50) or Color3.fromRGB(180,40,40)
    if espEnabled then updateESP() else clearAll(); warn.Visible = false end
end)

doorBtn.MouseButton1Click:Connect(function()
    showDoors = not showDoors; doorBtn.Text = showDoors and "门: ON" or "门: OFF"
    doorBtn.BackgroundColor3 = showDoors and Color3.fromRGB(50,180,50) or Color3.fromRGB(180,40,40)
end)

keyBtn.MouseButton1Click:Connect(function()
    showKeys = not showKeys; keyBtn.Text = showKeys and "钥匙: ON" or "钥匙: OFF"
    keyBtn.BackgroundColor3 = showKeys and Color3.fromRGB(50,180,50) or Color3.fromRGB(180,40,40)
end)

itemBtn.MouseButton1Click:Connect(function()
    showItems = not showItems; itemBtn.Text = showItems and "道具: ON" or "道具: OFF"
    itemBtn.BackgroundColor3 = showItems and Color3.fromRGB(50,180,50) or Color3.fromRGB(180,40,40)
end)

leverBtn.MouseButton1Click:Connect(function()
    showLevers = not showLevers; leverBtn.Text = showLevers and "拉杆: ON" or "拉杆: OFF"
    leverBtn.BackgroundColor3 = showLevers and Color3.fromRGB(50,180,50) or Color3.fromRGB(180,40,40)
end)

monsterBtn.MouseButton1Click:Connect(function()
    showMonsters = not showMonsters; monsterBtn.Text = showMonsters and "怪物: ON" or "怪物: OFF"
    monsterBtn.BackgroundColor3 = showMonsters and Color3.fromRGB(50,180,50) or Color3.fromRGB(180,40,40)
end)

nightBtn.MouseButton1Click:Connect(function()
    nightVision = not nightVision
    nightBtn.Text = nightVision and "夜视: ON" or "夜视: OFF"
    nightBtn.BackgroundColor3 = nightVision and Color3.fromRGB(50,180,50) or Color3.fromRGB(180,40,40)
    if not nightVision then Lighting.Brightness = 1; Lighting.FogEnd = 500; Lighting.ExposureCompensation = 0 end
end)

rayBtn.MouseButton1Click:Connect(function()
    rayEnabled = not rayEnabled
    rayBtn.Text = rayEnabled and "射线: ON" or "射线: OFF"
    rayBtn.BackgroundColor3 = rayEnabled and Color3.fromRGB(50,180,50) or Color3.fromRGB(180,40,40)
    if not rayEnabled then clearBeams() end
end)

print("Doors ESP 完整版加载完成")
