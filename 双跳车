-- 🌙 加一双跳自行车 | V1.5

local WI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()
if not WI then return end

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")

-- 变量
local autoWin = false
local flySpeed = 15000

-- ==================== 冰白主题 ====================
WI:AddTheme({
    Name = "冰白",
    Accent = "#80C0FF",
    Outline = "#6090C0",
    Text = "#FFFFFF",
    Placeholder = "#D0E0FF"
})

-- ==================== 飞行 ====================
local function flyToTarget(targetPos, label)
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then
        WI:Notify({Title = "失败", Content = "角色不存在", Duration = 2})
        return false
    end
    local hrp = char.HumanoidRootPart
    local distance = (targetPos - hrp.Position).Magnitude
    local duration = distance / flySpeed
    if duration < 0.1 then duration = 0.1 end

    local tween = TweenService:Create(hrp, TweenInfo.new(duration, Enum.EasingStyle.Linear), {CFrame = CFrame.new(targetPos)})
    tween:Play()
    tween.Completed:Wait()
    return true
end

-- ==================== 自动胜利（赢一次等30秒）====================
spawn(function()
    while true do
        if autoWin then
            pcall(function()
                local char = LocalPlayer.Character
                if not char or not char:FindFirstChild("HumanoidRootPart") then
                    task.wait(0.5)
                    return
                end
                local hrp = char.HumanoidRootPart
                local humanoid = char:FindFirstChild("Humanoid")
                if not humanoid then
                    task.wait(0.5)
                    return
                end

                local success = flyToTarget(Vector3.new(-71468, 1905.4, -41), "终点")
                if not success then return end
                task.wait(0.5)

                humanoid.WalkSpeed = 16
                humanoid:MoveTo(Vector3.new(-71471.4, 1906.3, 4.7))

                local startTime = tick()
                while tick() - startTime < 5 and autoWin do
                    local d = (hrp.Position - Vector3.new(-71471.4, 1906.3, 4.7)).Magnitude
                    if d < 3 then break end
                    task.wait(0.2)
                end

                humanoid.WalkSpeed = 16
                WI:Notify({Title = "自动胜利", Content = "已完成，等待30秒", Duration = 2})

                task.wait(30)
            end)
        end
        task.wait(0.5)
    end
end)

-- ==================== UI ====================
local Window = WI:CreateWindow({
    Title = "加一双跳自行车",
    Icon = "solar:moon-bold",
    Size = UDim2.fromOffset(500, 280),
    ToggleKey = Enum.KeyCode.RightShift,
    Theme = "冰白",
    Transparent = true,
    ScrollBarEnabled = true
})

Window:Tab({Title = "公告", Icon = "solar:info-circle-bold"}):Paragraph({
    Title = "黑脚本--加一双跳自行车",
    Desc = "飞行到终点(15000速) | 自动胜利(30秒间隔)\n别问为啥有间隔 因为奖杯就有间隔"
})

local T2 = Window:Tab({Title = "主要功能", Icon = "solar:star-bold"})
T2:Button({Title = "飞向终点", Justify = "Center", Callback = function()
    flyToTarget(Vector3.new(-71468, 1905.4, -41), "终点")
    WI:Notify({Title = "成功", Content = "已到终点", Duration = 2})
end})
T2:Toggle({Title = "自动胜利", Value = false, Callback = function(v) autoWin = v end})

local T3 = Window:Tab({Title = "设置", Icon = "solar:settings-bold"})
T3:Button({Title = "加载飞行脚本", Justify = "Center", Callback = function()
    pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/hgvuyguyg/jjjjjjjjj/main/飞饼"))() end)
    WI:Notify({Title = "飞行", Content = "已加载", Duration = 2})
end})
local statusParagraph = T3:Paragraph({Title = "功能状态", Desc = "加载中..."})

spawn(function()
    while true do
        pcall(function()
            local fps = math.floor(1 / (RunService.RenderStepped:Wait() or 0.016))
            local status = "FPS: " .. fps .. "\n\n" ..
                "自动胜利: " .. (autoWin and "✅" or "❌")
            statusParagraph:SetDesc(status)
        end)
        task.wait(0.5)
    end
end)

T3:Button({Title = "关闭面板", Justify = "Center", Callback = function() Window:Close() end})

Window:OnClose(function()
    autoWin = false
end)

WI:Notify({Title = "🌙 加一双跳自行车", Content = "加载完成 | 30秒间隔", Duration = 3})
print("🌙 加一双跳自行车 加载完成")
