-- 版本号: 1.0.0
local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
if not WindUI then return end

WindUI:AddTheme({
    Name = "冰白",
    Accent = "#80C0FF",
    Outline = "#6090C0",
    Text = "#FFFFFF",
    Placeholder = "#D0E0FF",
})

local Window = WindUI:CreateWindow({
    Title = "每次点击加一排水量",
    Icon = "solar:moon-bold",
    Size = UDim2.fromOffset(500, 350),
    ToggleKey = Enum.KeyCode.RightShift,
    Theme = "冰白",
    Transparent = true,
    ScrollBarEnabled = true,
})

-- 功能状态变量
local autoClickEnabled = false
local lastClickTime = 0

-- 公告标签页
Window:Tab({Title = "公告", Icon = "solar:info-circle-bold"}):Paragraph({
    Title = "加一排水量 v1.0.0",
    Desc = "主要功能：自动点击",
})

-- 主要功能标签页
local MainTab = Window:Tab({Title = "主要功能", Icon = "solar:star-bold"})

MainTab:Toggle({
    Title = "自动点击",
    Value = false,
    Callback = function(enabled)
        autoClickEnabled = enabled
        
        if enabled then
            WindUI:Notify({
                Title = "自动点击已开启",
                Content = "每0.01秒点击一次",
                Duration = 3
            })
        else
            WindUI:Notify({
                Title = "自动点击已关闭",
                Content = "已停止自动点击",
                Duration = 2
            })
        end
    end
})

-- 自动点击循环
game:GetService("RunService").Heartbeat:Connect(function()
    if autoClickEnabled then
        local currentTime = tick()
        if currentTime - lastClickTime >= 0.01 then
            local Event = game:GetService("ReplicatedStorage").Remote.Event.Level["[C-S]Click"]
            Event:FireServer(16)
            lastClickTime = currentTime
        end
    end
end)

-- 设置标签页
local SettingsTab = Window:Tab({Title = "设置", Icon = "solar:settings-bold"})

local statusParagraph = SettingsTab:Paragraph({
    Title = "功能状态",
    Desc = "正在加载..."
})

-- 更新状态显示
local function updateStatus()
    local status = "版本: v1.0.0\n\n"
    
    if autoClickEnabled then
        status = status .. "✅ 自动点击: 已开启\n"
    else
        status = status .. "❌ 自动点击: 未开启\n"
    end
    
    statusParagraph:SetDesc(status)
end

game:GetService("RunService").Heartbeat:Connect(function()
    updateStatus()
end)

SettingsTab:Button({
    Title = "关闭面板",
    Callback = function()
        Window:Close()
    end
})

WindUI:Notify({
    Title = "脚本加载完成",
    Content = "加一排水量 v1.0.0",
    Duration = 3
})
